import Panel from "../components/Panel";

const NoSuchPage = () => {
    return (
        <Panel title="Page doesn't exist">The URL you entered is not a valid address.</Panel>
    )
}

export default NoSuchPage;